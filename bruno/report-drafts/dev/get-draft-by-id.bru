meta {
  name: [DEV] Get report draft by id (no auth)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/dev/report-drafts/draft/{{draftId}}
  body: none
  auth: none
}

docs {
  # Dev only — NODE_ENV !== production

  Même JSON que GET /report-drafts/draft/:id avec JWT, sans token.
}
