# Déclare les variables dépôt / org utilisées dans les workflows (évite les faux positifs actionlint sur `vars.*`).
# https://github.com/rhysd/actionlint/blob/main/docs/config.md
config-variables:
  - CLIENT_SONARCLOUD_ENABLED
